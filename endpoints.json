{
    "GET /api": {
        "description": "Serves up a JSON representation of all the available endpoints of the weather API"
    },
    "GET /api/:location": {
        "description": "Displays the current weather at the specified location",
        "exampleResponse": {

        }
    }
}